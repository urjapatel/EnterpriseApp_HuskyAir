<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Reservation Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2>Reservation Details</h2>
            </div>
            <div class="card-body">
                <h4 class="card-title">
                    <span th:text="${reservation.firstName + ' ' + reservation.lastName}"></span>
                    <small class="text-muted">(ID: <span th:text="${reservation.id}"></span>)</small>
                </h4>
                
                <div class="row mb-3">
                    <div class="col-md-6">
                        <h5>Travel Information</h5>
                        <p>
                            <strong>Passengers:</strong> 
                            <span th:text="${reservation.numberOfPassengers}"></span><br>
                            <strong>Class:</strong> 
                            <span th:text="${reservation.travelClass}"></span><br>
                            <strong>Departure:</strong> 
                            <span th:text="${#temporals.format(reservation.dateOfDeparture, 'MMMM dd, yyyy')}"></span>
                            at <span th:text="${#temporals.format(reservation.time, 'HH:mm')}"></span>
                        </p>
                    </div>
                    <div class="col-md-6">
                        <h5>Contact Information</h5>
                        <p>
                            <strong>Phone:</strong> 
                            <span th:text="${reservation.phoneNumber}"></span><br>
                            <strong>Customer Address:</strong> 
                            <span th:text="${reservation.customer?.address} ?: 'N/A'"></span>
                        </p>
                    </div>
                </div>

                <div th:if="${reservation.ticket}" class="mb-3">
                    <h5>Ticket Information</h5>
                    <p>
                        <strong>Ticket #:</strong> 
                        <span th:text="${reservation.ticket.number}"></span><br>
                        <strong>Price:</strong> 
                        <span th:text="${'$' + #numbers.formatDecimal(reservation.ticket.price, 1, 2)}"></span>
                    </p>
                </div>

                <div class="mt-4">
                    <a th:href="@{'/reservations/edit/' + ${reservation.id}}" class="btn btn-warning">
                        <i class="bi bi-pencil"></i> Edit
                    </a>
                    <form th:action="@{'/reservations/delete/' + ${reservation.id}}" method="post" style="display: inline-block;">
                        <button type="submit" class="btn btn-danger" 
                                onclick="return confirm('Are you sure you want to delete this reservation?')">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </form>
                    <a href="/reservations" class="btn btn-secondary">
                        <i class="bi bi-list"></i> Back to List
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>